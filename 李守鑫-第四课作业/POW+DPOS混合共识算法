POW+DPOS混合共识算法
1.前期使用pow挖矿，节点少，系统本身会获得大量矿工的节点，便于管理，可以在前期提供系统安全性，并向生成区块的矿工节点发送奖励
2.当进入挖矿后期，比如区块达到一定高度，或者根据项目的进展情况，开始引入DPOS共识，采用POW打包写账本，DPOS用于投票治理的机制。
POS矿工通过他们的选票生成一个投票交易在区块上进行投票，POW矿工开始构建一个区块并插入POS矿工的投票，POW矿工会在新区块中通过
某个标识来检查POS矿工的交易是否有效，如果有效，那么会生产比特位标识，POW矿工会计算出满足网络难度的随机值，将此块添加到区块链中。

